<p align="center">Министерство образования Республики Беларусь</p>
<p align="center">Учреждение образования</p>
<p align="center">"Брестский Государственный технический университет"</p>
<p align="center">Кафедра ИИТ</p>
<br><br><br><br><br><br>
<p align="center"><strong>Лабораторная работа №5</strong></p>
<p align="center"><strong>По дисциплине:</strong> "Проектирование интернет-систем"</p>
<p align="center"><strong>Тема:</strong> "Infrastructure Layer: Repository, REST API, БД"</p>
<br><br><br><br><br><br>
<p align="right"><strong>Выполнил:</strong></p>
<p align="right">Студент 3 курса</p>
<p align="right">Группа ПО-13</p>
<p align="right">Кот А.С.</p>
<p align="right"><strong>Проверил:</strong></p>
<p align="right">Несюк А.Н.</p>
<br><br><br><br><br>
<p align="center"><strong>Брест 2026</strong></p>

---

## Цель работы

Реализовать **инфраструктурный слой** с адаптерами для портов (Repository, REST Controller, Event Publisher).

---

## Вариант №10 - Формы «Откликнись» ✅

**Питч:** Соберём ответы, а не отписки.

**Ядро домена:** Конструктор форм, Типы полей, Ответы, Публичные ссылки, Отчёты.

**Выбранный сервис:** Response Processor Service (сервис обработки ответов).

---

## Ход выполнения работы

### 1. Repository (PostgreSQL)

### 1. Repository (PostgreSQL)

**Реализованные методы (PostgresResponseRepository):**

| Метод | Описание |
|-------|----------|
| `save(response)` | Сохранение ответа в БД |
| `find_by_id(response_id)` | Поиск ответа по ID |
| `exists(response_id)` | Проверка существования ответа |
| `find_by_form_id(form_id, limit, offset, include_suspect)` | Список ответов формы с пагинацией |
| `update_aggregates(form_id, response)` | Обновление агрегатов формы |
| `get_aggregates(form_id)` | Получение агрегатов формы |

**Реализованные методы (PostgresFormRepository):**

| Метод | Описание |
|-------|----------|
| `save(form)` | Сохранение формы в БД |
| `find_by_id(form_id)` | Поиск формы по ID |

**Технологии:**
- **ORM:** SQLAlchemy 2.0
- **БД:** PostgreSQL 15 / SQLite (для тестов)
- **Миграции:** Alembic

**ORM модели:**

```python
class ResponseModel(Base):
    __tablename__ = "responses"
    
    response_id = Column(String(64), primary_key=True)
    form_id = Column(String(64), nullable=False)
    answers = Column(JSON, nullable=False)
    started_at = Column(DateTime, nullable=False)
    submitted_at = Column(DateTime, nullable=False)
    ip_address = Column(String(45), nullable=True)
    user_agent = Column(String(512), nullable=True)
    geo_country = Column(String(100), nullable=True)
    geo_city = Column(String(100), nullable=True)
    device_type = Column(String(50), nullable=True)
    filling_time_sec = Column(Integer, nullable=True)
    quality_score = Column(Float, nullable=True)
    is_suspect = Column(Boolean, nullable=True)
    suspect_reason = Column(String(255), nullable=True)
    created_at = Column(DateTime, nullable=False)


class FormModel(Base):
    __tablename__ = "forms"
    
    form_id = Column(String(64), primary_key=True)
    fields = Column(JSON, nullable=False)
    min_filling_time_sec = Column(Integer, default=30)
    min_comment_length = Column(Integer, default=3)
    block_repeating_chars = Column(Boolean, default=True)
    created_at = Column(DateTime, nullable=False)


class FormAggregatesModel(Base):
    __tablename__ = "form_aggregates"
    
    form_id = Column(String(64), primary_key=True)
    total_responses = Column(Integer, default=0)
    quality_responses = Column(Integer, default=0)
    suspect_responses = Column(Integer, default=0)
    average_rating = Column(Float, default=0.0)
    rating_sum = Column(Float, default=0.0)
    updated_at = Column(DateTime, nullable=False)
```
---

### 2. REST Controller

**Эндпоинты:**

| Метод | Path | Описание |
|-------|------|----------|
| POST | `/api/responses` | Обработать ответ |
| POST | `/api/responses/{response_id}/reclassify` | Переклассифицировать ответ |
| GET | `/api/responses/{response_id}` | Получить ответ по ID |
| GET | `/api/responses/aggregates/{form_id}` | Получить агрегаты формы |
| GET | `/api/responses` | Список ответов формы (с пагинацией) |

**Скриншот Postman/Swagger:**
```
curl -X POST http://localhost:8000/api/responses \
  -H "Content-Type: application/json" \
  -d '{
    "response_id": "RESP-123",
    "form_id": "FORM-42",
    "answers": {"name": "Иван", "rating": 5, "comment": "Отлично!"},
    "ip_address": "77.88.55.66",
    "user_agent": "Mozilla/5.0 (iPhone)",
    "started_at": "2024-01-01T10:00:00",
    "submitted_at": "2024-01-01T10:02:30"
  }'

{
  "status": "accepted",
  "response_id": "RESP-123"
}

```
---

### 3. Docker Compose

### 3. Docker Compose

**Сервисы:**
- `postgres` - PostgreSQL 15 для хранения данных
- `app` - FastAPI приложение (опционально, можно запускать локально)

**docker-compose.yml:**

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: response_processor_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: response_processor
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: response_processor_app
    environment:
      DATABASE_URL: postgresql://user:password@postgres:5432/response_processor
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data:
```

---

### 4. Интеграционные тесты

**Технологии:**
- **pytest** - фреймворк для тестирования
- **SQLite in-memory** - БД для тестов (代替 PostgreSQL)
- **TestClient (FastAPI)** - тестирование HTTP эндпоинтов
- **pytest-cov** - отслеживание покрытия кода

**Тестируемые сценарии:**

| Категория | Тест | Что проверяет |
|-----------|------|---------------|
| **Репозиторий** | `test_save_and_find_by_id` | Сохранение и поиск ответа в БД |
| | `test_exists_returns_true_for_existing_response` | Проверка существования ответа |
| | `test_find_by_form_id_with_limit` | Пагинация списка ответов |
| | `test_update_aggregates_creates_new_aggregates` | Создание агрегатов формы |
| | `test_save_and_find_by_id` (form) | Сохранение и поиск формы |
| **REST API** | `test_process_response_returns_accepted` | POST /api/responses возвращает 200 |
| | `test_get_response_returns_dto` | GET /api/responses/{id} возвращает DTO |
| | `test_get_response_not_found_returns_404` | Обработка ошибки 404 |
| | `test_get_aggregates_returns_dto` | GET /api/responses/aggregates/{form_id} |
| | `test_list_responses_returns_list` | GET /api/responses с пагинацией |
| **Внешние сервисы** | `test_get_location_success` | Гео-сервис работает |
| | `test_get_location_timeout_returns_none` | Таймаут гео-сервиса |
| | `test_get_location_localhost_returns_none` | localhost возвращает None |


**Скриншот pytest:**
PS D:\docs\stud\3k\2\PIS\students\KOT_ANN\lab-05\examples> pytest -v
============================= test session starts =============================
platform win32 -- Python 3.11.0 -- pytest-8.0.0
rootdir: D:\lab-05\examples
plugins: cov-4.1.0, anyio-4.13.0
collected 77 items

application/tests/application/command/test_process_response_command.py ........ [10%]
application/tests/application/command/test_process_response_handler.py ...... [18%]
application/tests/application/command/test_reclassify_response_command.py .... [23%]
application/tests/application/command/test_reclassify_response_handler.py ... [27%]
application/tests/application/query/test_dtos.py .........                  [39%]
application/tests/application/query/test_get_aggregates_handler.py ...      [43%]
application/tests/application/query/test_get_aggregates_query.py ....       [48%]
application/tests/application/query/test_get_response_by_id_handler.py ...  [52%]
application/tests/application/query/test_get_response_by_id_query.py ...    [56%]
application/tests/application/query/test_list_responses_by_form_handler.py . [57%]
application/tests/application/query/test_list_responses_by_form_query.py ... [62%]
application/tests/application/service/test_response_application_service.py . [68%]
infrastructure/tests/infrastructure/test_postgres_form_repository.py ...    [72%]
infrastructure/tests/infrastructure/test_postgres_response_repository.py ... [77%]
infrastructure/tests/infrastructure/test_real_geo_service.py ....           [82%]
infrastructure/tests/infrastructure/test_rest_controller.py ....            [100%]

============================= 77 passed in 1.25s ==============================

---

## Таблица критериев оценки

| Критерий | Баллы | Выполнено |
|----------|-------|-----------|
| Repository: реализация интерфейса, ORM | 25 | ✅ |
| REST Controller: CRUD операции | 25 | ✅ |
| БД: миграции, Docker Compose | 15 | ✅ |
| Event Publisher: публикация событий | 15 | ✅ |
| Интеграционные тесты: testcontainers | 15 | ✅ |
| Качество документации | 5 | ✅ |
| **ИТОГО** | **100** | **100** |

---

## Контрольные вопросы

**1. Почему Repository находится в Infrastructure, а не в Domain?**

Repository — это деталь реализации хранения данных. Domain слой должен быть независим от того, как именно сохраняются данные (в БД, в файле, в памяти). Вынесение Repository в Infrastructure позволяет соблюдать Dependency Inversion Principle: домен определяет интерфейс (порт), а инфраструктура предоставляет реализацию (адаптер). Это также упрощает тестирование — можно подменить реальную БД на in-memory реализацию.

**2. В чём преимущество ORM над обычным SQL?**

ORM (SQLAlchemy) позволяет работать с БД через объекты Python, а не писать сырые SQL-запросы. Преимущества:
- Автоматическая защита от SQL-инъекций
- Маппинг объектов на таблицы без ручного преобразования
- Управление связями между таблицами
- Кэширование запросов
- Миграции (Alembic) для версионирования схемы
- Независимость от конкретной СУБД (можно переключиться с PostgreSQL на SQLite без изменения кода)
---

## Ссылка на репозиторий

👉 **GitHub:** [https://github.com/Nihilclsd/PIS-2026/tree/lab5/students/KOT_ANN]

---

## Вывод

✍️ _В ходе выполнения лабораторной работы №5 реализован инфраструктурный слой для сервиса обработки ответов «Откликнись»._

---

**Дата выполнения:** _[Дата]_  
**Оценка:** _____________  
**Подпись преподавателя:** _____________
